---
"@signalridge/pi-ask-user-question": patch
"@signalridge/pi-lsp": patch
"@signalridge/pi-tab-status": patch
"@signalridge/pi-code-actions": patch
"@signalridge/pi-usage-extension": patch
"@signalridge/pi-files-widget": patch
---

Cancel RPC question dialogs at the host, serialize LSP fixes with native file edits when the host provides a mutation queue, resolve workspace paths against session context, track successful shell commits, and report terminal-only commands explicitly over RPC.
